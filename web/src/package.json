{
  "name": "ccmux-frontend",
  "private": true,
  "scripts": {
    "build:xterm": "esbuild xterm-entry.js --bundle --minify --outfile=../static/dist/xterm-bundle.js",
    "build:codemirror": "esbuild codemirror-entry.js --bundle --minify --outfile=../static/dist/codemirror-bundle.js",
    "build": "npm run build:xterm"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
